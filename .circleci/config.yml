version: 2.1

jobs:
  print-hello:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - checkout
      - run: echo "hello!"

  print-world:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - run: echo "world!"

workflows:
  my-workflow:
    jobs:
      - print-hello
      - print-world:
          requires:
            - print-hello